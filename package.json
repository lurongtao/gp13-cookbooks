{
  "name": "cookbooks",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "antd-mobile": "^2.3.1",
    "better-scroll": "^1.15.2",
    "react": "16.8",
    "react-dom": "16.8",
    "react-scripts": "3.2.0",
    "styled-components": "^4.4.1"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom",
    "eject": "react-scripts eject",
    "mock:hotcate": "json-server ./src/mock/cookbook-category-hot.json --port 9000 --watch",
    "mock": "json-server ./src/mock/mock.js -r ./src/mock/route.json --port 9000 --watch"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "babel-plugin-import": "^1.12.2",
    "customize-cra": "^0.8.0",
    "react-app-rewired": "^2.1.5"
  }
}
